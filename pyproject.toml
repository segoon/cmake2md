[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "cmake2md"
dynamic = ["version"]
description = "Documentation generator for CMake: extracts doxygen-like comments and renders them with Jinja templates"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
authors = [{ name = "Vasiliy Kulikov", email = "segooon@gmail.com" }]
keywords = ["cmake", "documentation", "doxygen", "jinja", "markdown"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Documentation",
    "Topic :: Software Development :: Build Tools",
    "Typing :: Typed",
]
dependencies = [
    "jinja2>=3.0,<4",
    # QueryCursor and the Query(language, source) constructor are 0.25 API.
    "tree-sitter>=0.25,<0.26",
    # Must be ABI-compatible with the tree-sitter core above; a mismatch
    # surfaces as "Incompatible Language version" at import time.
    "tree-sitter-cmake>=0.5",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "mypy>=1.8",
    "ruff>=0.6",
    "build>=1.0",
    "twine>=5.0",
]

[project.scripts]
cmake2md = "cmake2md.cli:main"

[project.urls]
Homepage = "https://github.com/segoon/cmake2md"
Source = "https://github.com/segoon/cmake2md"
Issues = "https://github.com/segoon/cmake2md/issues"
Changelog = "https://github.com/segoon/cmake2md/blob/master/CHANGELOG.md"

# Single source of truth for the version: cmake2md.__version__, which
# --version reports.
[tool.hatch.version]
path = "src/cmake2md/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/cmake2md"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]

[tool.ruff.lint.isort]
force-single-line = true
order-by-type = false

[tool.ruff.format]
quote-style = "single"

[tool.mypy]
python_version = "3.10"
files = ["src", "tests"]
strict = true
# tests/ has no __init__.py, so name its modules after the directory anyway
# ("tests.test_cli" rather than "test_cli") to keep the override below matching.
mypy_path = ["src", "."]
explicit_package_bases = true

[[tool.mypy.overrides]]
module = ["tree_sitter_cmake.*"]
ignore_missing_imports = true

# Tests need no annotations of their own: check_untyped_defs stays on, so their
# bodies are still checked against the annotated API in src.
[[tool.mypy.overrides]]
module = ["tests.*"]
disallow_untyped_defs = false
disallow_incomplete_defs = false
disallow_untyped_calls = false
